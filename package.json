{
  "name": "origin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-contrast.js && node scripts/check-registry.js",
    "build": "vite build",
    "preview": "vite preview",
    "check:contrast": "node scripts/check-contrast.js",
    "check:tokens": "node scripts/check-tokens.js",
    "check:registry": "node scripts/check-registry.js",
    "check:all": "node scripts/check-contrast.js && node scripts/check-registry.js && node scripts/check-tokens.js"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "lucide-react": "^1.14.0",
    "pdf-lib": "^1.17.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "sharp": "^0.34.5",
    "vite": "^5.0.0"
  }
}
